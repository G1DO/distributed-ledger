\* Same finite state space. Fair completion, with no assumption of new calls.
CONSTANTS
    Keys = {KeyA, KeyB}
    Values = {Empty, Nonempty}
    Absent = Absent
SPECIFICATION FairSpec
INVARIANTS TypeOK InvocationUnchanged ResponseCorrect OperationEffect OperationFrame
PROPERTY RequestProgress
CHECK_DEADLOCK TRUE
