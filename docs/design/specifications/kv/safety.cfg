\* Exhaustive finite safety: two keys, two string values, and distinct absence.
\* Empty models the empty string; Nonempty models another valid string.
CONSTANTS
    Keys = {KeyA, KeyB}
    Values = {Empty, Nonempty}
    Absent = Absent
INIT Init
NEXT Next
INVARIANTS TypeOK InvocationUnchanged ResponseCorrect OperationEffect OperationFrame
CHECK_DEADLOCK TRUE
